{
  "name": "story-customization-platform-backend",
  "version": "1.0.0",
  "description": "Backend for a story customization platform similar to WonderWraps",
  "main": "src/app.js",
  "scripts": {
    "start": "cd src && node app.js",  // Ensures the app is started from the 'src' directory
    "dev": "cd src && nodemon app.js", // Ensures nodemon runs from the 'src' directory
    "test": "jest"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.10.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.31.0",
    "sharp": "^0.32.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "nodemon": "^2.0.22",
    "supertest": "^6.3.3"
  }
}
